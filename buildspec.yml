version: 0.2

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - npm run build # Builds the project
artifacts:
  files:
    - "**/*"
  base-directory: "build"
  s3-bucket: saurab-ghimire-react-codepipeline-s3
